#include<stdio.h>

void main(){
	char sent[100];
	int i, len, total;
	
	gets(sent);
	len=strlen(sent);
	j=0;
	for(i=0; i<len; i++){
		if(sent[i]>=65&&sent[i]<=91){
			a[j]=sent[i];
			j++;
		}
		if((sent[i]>=48&&sent[i]<=57)&&(sent[i+1]!=) ){
			a[j]=sent[i];
		}
	}
}
